# LEGACY_QUARANTINE: archival recovery reference only. This filename is not
# auto-discovered by Wrangler; use the active apps/web/wrangler.jsonc instead.
name = "va-freelance-hub-legacy-archive"
pages_build_output_dir = ".open-next"
main = "./entry.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

[[d1_databases]]
binding = "DB"
database_name = "remoteph-jobs-db"
database_id = "00000000-0000-0000-0000-000000000000"

[ai]
binding = "AI"

[triggers]
crons = ["0 6,18 * * *", "0 1 * * *", "0 2 * * 0"]
