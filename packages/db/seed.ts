/**
 * LEGACY_QUARANTINE
 *
 * The original hand-maintained directory seed is stale and bypasses source
 * verification. Recover it only through a reviewed D1 migration or a
 * compliant ingestion source on an isolated branch.
 */
throw new Error("LEGACY_QUARANTINE: historical directory seed is disabled.");
